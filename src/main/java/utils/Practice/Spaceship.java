package utils.Practice;

//an interface example
//an interface is a reference type in Java, similar to class, that can contain only constants, method signatures, default methods, static methods, and nested types. Interfaces cannot contain instance fields. The methods in interfaces are abstract by default
// It's implemented in class UFO

public interface Spaceship {
    void takeOff();
    void dock();
}
